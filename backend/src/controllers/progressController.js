export const getProgressEntries = async (req, res) => {
  res.json({ message: 'Get progress entries endpoint placeholder', entries: [] });
};

export const createProgressEntry = async (req, res) => {
  res.status(201).json({ message: 'Create progress entry endpoint placeholder' });
};

